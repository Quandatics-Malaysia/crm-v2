import "server-only"

// Payment milestones intentionally have no Finance or Project-completion
// service. Finance documents remain independently usable, while milestone
// state is owned by the CRM lifecycle and its stage/actions services.
